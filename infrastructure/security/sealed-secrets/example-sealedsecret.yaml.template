apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: example-secret
  namespace: platform
spec:
  encryptedData:
    username: REPLACE_WITH_ENCRYPTED
    password: REPLACE_WITH_ENCRYPTED

# This is a template — encrypt values with `kubeseal` using the controller
# public key or `kubeseal` client installed in your environment.
